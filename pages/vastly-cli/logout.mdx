# vastly <em> logout</em>
import { Callout } from 'nextra-theme-docs'

The `vastly logout` command allows you to logout to your Vastly account through [Vastly CLI](/vastly-cli).

<br/>

# Usage
  ```bash copy filename="terminal"
  vastly logout
  ```